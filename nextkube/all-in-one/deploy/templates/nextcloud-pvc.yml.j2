kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: nextcloud-data
  labels:
    ncInstanceID: "{{ nc_instance_id }}"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
